services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: teacheros
      POSTGRES_PASSWORD: teacheros
      POSTGRES_DB: teacheros
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U teacheros -d teacheros"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
